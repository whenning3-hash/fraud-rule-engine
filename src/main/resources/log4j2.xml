<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <!-- %X{correlationId} reads the MDC key set by CorrelationIdFilter for every HTTP
             request.  Non-request logs (Flyway, startup) have no request context so the
             brackets are empty — that is expected and distinguishes infra-level logs from
             request-level ones.  Do NOT use %X{key:-default} — Log4j2 PatternLayout treats
             the entire string between braces as the literal key name; the ':-' suffix is only
             meaningful in configuration Lookups, not in pattern conversion specifiers. -->
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} [%X{correlationId}] - %msg%n</Property>
        <Property name="LOG_PATTERN_JSON">{"timestamp":"%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}","level":"%p","thread":"%t","logger":"%c{1}","correlationId":"%X{correlationId}","message":"%enc{%m}{JSON}"}%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>
    </Appenders>

    <Loggers>
        <Logger name="za.co.fraudruleengine" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Logger name="org.springframework" level="WARN" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Logger name="org.hibernate.SQL" level="WARN" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Root level="WARN">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>
