fraud:
  security:
    enabled: false
  score:
    # Lower threshold for local/demo: individual rules trigger alerts so Bruno/Postman tests are easy to observe.
    # Production default is 60 (requires combined signals).
    threshold: 20

spring:
  jpa:
    show-sql: true

logging:
  config: classpath:log4j2-local.xml
